# Validation fixtures

This directory will contain independently derived golden records and external
adapter fixtures. Package-generated outputs must never be used as their own
golden expectations.

No scientific fixture is included in the pre-CRAN scaffold.

